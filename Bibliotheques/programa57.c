#include <stdio.h>
#include <math.h>

int main(){

	double a = 0;
	double b = 1;
	double c = 2;

	printf("L'exponentielle de %lf est %lf\n", a, exp(a));
	printf("L'exponentielle de %lf est %lf\n", b, exp(b));
	printf("L'exponentielle de %lf est %lf\n", c, exp(c));

	return 0;
}
